# Force devDependencies to be installed even when NODE_ENV=production
# (Render sets NODE_ENV=production, which otherwise omits devDeps the build
# needs: typescript, @types/*, vite, vitest).
include=dev
